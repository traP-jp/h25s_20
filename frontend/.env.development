# 開発環境設定
# このファイルは開発環境でのみ読み込まれます

# 通常は config/app.ts で自動設定されるため、これらの設定は不要です
# 特別な理由がある場合のみコメントアウトして使用してください

# VITE_API_BASE_URL=http://localhost:8080/api
# VITE_WS_BASE_URL=ws://localhost:8080/api/ws

# デバッグ用設定
# VITE_DEBUG=true
