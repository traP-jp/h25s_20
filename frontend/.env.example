# 環境変数設定例
# このファイルをコピーして .env.local を作成し、個人の開発環境に合わせて設定してください

# === 基本設定 ===
# 通常は .env.development と .env.production で自動設定されるため、
# 特別な設定が必要な場合のみ .env.local で上書きしてください

# カスタムAPIベースURL（オプション）
# VITE_API_BASE_URL=http://localhost:8080/api

# カスタムWebSocketベースURL（オプション）
# 指定しない場合はAPI_BASE_URLから自動生成されます
# VITE_WS_BASE_URL=ws://localhost:8080/api/ws

# === 使用例 ===

# 例1：別のローカルサーバーを使用する場合
# VITE_API_BASE_URL=http://localhost:3000/api
# VITE_WS_BASE_URL=ws://localhost:3000/api/ws

# 例2：ステージング環境を使用する場合
# VITE_API_BASE_URL=https://staging.example.com/api
# VITE_WS_BASE_URL=wss://staging.example.com/api/ws

# 例3：開発中に本番環境をテストする場合
# VITE_API_BASE_URL=https://10ten.trap.show/api
# VITE_WS_BASE_URL=wss://10ten.trap.show/api/ws

# === ファイルの使い分け ===
# .env.local        - 個人のローカル設定（Git管理対象外）
# .env.development  - 開発環境共通設定（Git管理対象）
# .env.production   - 本番環境設定（Git管理対象）
